# requirements.txt — ShopFloorScheduler
# Generated for Phase 1 (Architecture Refactor)

# ─── Existing dependencies ───────────────────────────────────────────────────
flask>=3.0.0                   # Legacy (kept for reference; replaced by FastAPI)
pandas>=2.0.0
openpyxl>=3.1.0
gspread>=6.0.0
matplotlib>=3.8.0
xlsxwriter>=3.1.0

# ─── TASK-01: FastAPI + ASGI server ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # ASGI server with websocket + http/2 support
python-multipart>=0.0.9        # Required for FastAPI file upload (Form + UploadFile)

# ─── TASK-03: Celery + Redis task queue ──────────────────────────────────────
celery>=5.4.0
redis>=5.0.0                   # Python Redis client (also used as Celery backend)

# ─── TASK-04: Pydantic (FastAPI uses it internally; explicit pin for stability) ─
pydantic>=2.7.0

# ─── TASK-05: Structured logging ─────────────────────────────────────────────
loguru>=0.7.2

# ─── TASK-13: Database (SQLAlchemy ORM + SQLite/PostgreSQL) ──────────────────
sqlalchemy>=2.0.0        # ORM + connection pooling
alembic>=1.13.0          # Schema migrations
aiosqlite>=0.20.0        # Async SQLite driver (needed for SQLAlchemy async mode)
psycopg2-binary>=2.9.0         # PostgreSQL driver (Phase 3)

# ─── Phase 3: Authentication & Rate Limiting ──────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT token creation / validation
passlib[bcrypt]>=1.7.4             # Password hashing (bcrypt backend)
slowapi>=0.1.10                    # Rate limiting for auth routes

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                  # Required for FastAPI TestClient

# ─── Phase 4: Predictive Maintenance ML ──────────────────────────────────────
scikit-learn>=1.4.0            # Isolation Forest anomaly detection
numpy>=1.26.0                  # Array operations for sklearn

# ─── Phase 5: PDF Report Export ──────────────────────────────────────────────
reportlab>=4.0.0               # PDF generation (cover page, tables, embedded Gantt)
